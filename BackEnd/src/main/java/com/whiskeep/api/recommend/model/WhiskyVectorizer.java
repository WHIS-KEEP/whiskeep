package com.whiskeep.api.recommend.model;

import java.util.Map;

import com.whiskeep.api.whisky.domain.Whisky;
import com.whiskeep.common.model.TastingProfile;
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter
@NoArgsConstructor
@AllArgsConstructor
public class WhiskyVectorizer {

	public double[] vetorizeWhisky(TastingProfile<Map<String, Double> profile,>) {
		TastingProfile<Map<String, Double>> nosing = whisky.getNosing().;
		TastingProfile<Map<String, Double>> tasting = whisky.getTasting();
		TastingProfile<Map<String, Double>> finish = whisky.getFinish();
		whisky.getNosing().
	}
}
